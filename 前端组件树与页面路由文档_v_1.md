# 前端组件树与页面路由文档（V1）

## 1. 文档信息

- 文档名称：前端组件树与页面路由文档
- 所属产品：CogTree / 模型树阅读法
- 文档类型：前端工程设计文档
- 版本：V1.0
- 文档目标：明确前端页面路由、页面壳层、组件树结构、共享组件边界、状态管理分层、命令层挂载位置与第一阶段工程落地方案

---

## 2. 文档结论

## 2.1 一句话结论

前端建议采用：

**页面路由清晰分层 + 统一 AppShell + 三大核心页面共享画布引擎 + 状态按文档/画布/UI/历史分层管理**

即：
- 页面上采用稳定的主路由结构
- 布局上采用统一工作台壳层
- 画布能力做成共享引擎层，而不是散落在页面内部
- 命令系统独立一层，避免逻辑塞进组件

## 2.2 为什么需要单独写这份文档

前面你已经有了：
- 总 PRD
- 子页面 PRD
- 画布区专项 PRD
- 后端架构
- Tree First 数据流与命令系统文档
- 数据库设计
- API 接口设计

但如果没有前端组件树与路由文档，真正落地开发时仍然会出现问题：
- 页面结构容易越写越乱
- 共享组件边界不清
- 画布逻辑被页面侵蚀
- store 职责混乱
- 命令层分散在各处

所以这份文档的目标就是：

**把产品文档翻译成可执行的前端工程骨架。**

---

## 3. 前端总体架构结论

## 3.1 技术栈结论

前端建议采用：

- React
- TypeScript
- Vite
- React Router
- Zustand
- TanStack Query
- `@xyflow/react`（React Flow）
- Tiptap（仅右侧富文本区 / 复盘区）
- Tailwind CSS
- shadcn/ui

## 3.2 架构原则

### 原则 1：页面壳层统一
三大核心页面共享一个统一的 AppShell。

### 原则 2：画布引擎共享
金句提炼页、书内修剪台、主题整理页的画布不是三套独立实现，而是共享一套 Canvas Workspace 能力。

### 原则 3：命令逻辑不进入组件内部
组件只负责交互与呈现，结构修改通过 command layer 执行。

### 原则 4：Tree First 前端单真相源
前端只维护一份可编辑树结构真相源，React Flow nodes / edges 必须由 tree 派生。

### 原则 5：页面、模块、组件逐层收敛
不允许“页面里直接塞所有逻辑”，而要严格分层。

---

## 4. 页面路由设计

## 4.1 路由总览

建议采用如下一级路由：

```txt
/
/login
/app
/app/books
/app/quotes
/app/book-tree/:bookId
/app/theme-tree/:themeId
/app/reviews
/app/settings
```

## 4.2 路由说明

### `/login`
登录页。

### `/app`
应用主壳层入口，进入后统一挂载：
- 顶部全局栏
- 一级导航栏
- 页面内容区

### `/app/books`
书籍页。

### `/app/quotes`
金句提炼页入口。
可通过 query 参数保留上下文：
- `bookId`
- `quoteId`

### `/app/book-tree/:bookId`
书内修剪台。

### `/app/theme-tree/:themeId`
主题整理页。

### `/app/reviews`
复盘页。

### `/app/settings`
设置页。

---

## 4.3 路由参数建议

### 金句提炼页
`/app/quotes?bookId=xxx&quoteId=xxx`

### 书内修剪台
`/app/book-tree/:bookId?theme=wealth&nodeId=xxx`

### 主题整理页
`/app/theme-tree/:themeId?bookIds=xxx,yyy&nodeId=zzz`

### 复盘页
`/app/reviews?type=theme&themeId=wealth`

## 4.4 为什么这样设计

原因：
- URL 能表达当前工作上下文
- 支持刷新恢复
- 支持页面间跳转保留上下文
- 支持复制链接定位到具体页面状态

---

## 5. AppShell 设计

## 5.1 AppShell 的角色

AppShell 是整个应用的统一工作台壳层。

它负责：
- 顶部全局操作栏
- 最左侧一级导航栏
- 页面主区域挂载
- 全局浮层
- Toast / Modal / CommandBar 等全局组件

## 5.2 AppShell 组件树建议

```txt
App
└── RouterProvider
    └── AppShell
        ├── GlobalTopBar
        ├── PrimarySidebar
        ├── MainWorkspaceOutlet
        ├── GlobalToastLayer
        ├── GlobalModalLayer
        └── GlobalCommandBar（后期）
```

## 5.3 AppShell 不负责什么

AppShell 不应负责：
- 页面级业务数据拉取
- 树结构编辑
- 节点详情逻辑
- 页面专属命令

这些应交给页面和模块层。

---

## 6. 页面层设计

## 6.1 页面层原则

页面层负责：
- 拼装本页布局
- 调用本页 query
- 连接本页 command action
- 组织左右中三区域

页面层不负责：
- 深层画布逻辑
- 节点结构算法
- 命令执行细节

## 6.2 页面组件建议

### 书籍页
- `BooksPage`

### 金句提炼页
- `QuotesPage`

### 书内修剪台
- `BookTreePage`

### 主题整理页
- `ThemeTreePage`

### 复盘页
- `ReviewsPage`

### 设置页
- `SettingsPage`

---

## 7. 三大核心页面组件树

## 7.1 金句提炼页组件树

```txt
QuotesPage
├── PageTopContextBar
├── QuotesPageLayout
│   ├── SecondaryResourcePanel
│   │   ├── ResourcePanelHeader
│   │   ├── BookLibraryTabs
│   │   ├── QuoteSearchInput
│   │   ├── QuoteStatusFilter
│   │   └── QuoteList
│   ├── QuoteExtractionWorkspace
│   │   ├── ReadingGoalBar
│   │   ├── CurrentQuoteCard
│   │   ├── KeywordEditor
│   │   ├── CauseEffectEditor
│   │   ├── ExtractionCanvasSection
│   │   │   └── CanvasWorkspace
│   │   └── PushToBookTreeActionBar
│   └── QuoteRightPanel
│       ├── TabsContainer
│       ├── ExtractionSummaryCard
│       ├── CandidateThemeSelector
│       ├── PendingQuestionCard
│       └── NotesCard
```

## 7.2 书内修剪台组件树

```txt
BookTreePage
├── PageTopContextBar
├── BookTreePageLayout
│   ├── SecondaryResourcePanel
│   │   ├── ResourcePanelHeader
│   │   ├── ThemeFilterBar
│   │   ├── QueueCardList
│   │   ├── ChapterTree
│   │   └── NodePoolList
│   ├── BookTreeWorkspace
│   │   ├── BookTreeToolbar
│   │   └── CanvasWorkspace
│   └── BookTreeRightPanel
│       ├── TabsContainer
│       ├── BookNodeDefinitionCard
│       ├── PruningSuggestionCard
│       ├── SourceQuoteCardList
│       └── NotesCard
```

## 7.3 主题整理页组件树

```txt
ThemeTreePage
├── PageTopContextBar
├── ThemeTreePageLayout
│   ├── SecondaryResourcePanel
│   │   ├── ResourcePanelHeader
│   │   ├── SourceSearchInput
│   │   ├── SourceBookList
│   │   └── AddSourceButton
│   ├── ThemeTreeWorkspace
│   │   ├── ThemeOverviewBar
│   │   └── CanvasWorkspace
│   └── ThemeTreeRightPanel
│       ├── TabsContainer
│       ├── ThemeDefinitionCard
│       ├── ThemeInsightCard
│       ├── ThemeSourceCard
│       ├── ThemeRelatedCard
│       └── NotesCard
```

---

## 8. Canvas Workspace 共享层设计

## 8.1 Canvas Workspace 的定位

`CanvasWorkspace` 是三大页面共用的画布引擎挂载层。

它不直接理解“这是金句页还是主题页”，而是通过 props / adapter 获取上下文。

## 8.2 Canvas Workspace 组件树建议

```txt
CanvasWorkspace
├── CanvasToolbar
├── FloatingToolPalette
├── CanvasViewportLayer
│   ├── NodeRendererLayer
│   ├── EdgeRendererLayer
│   ├── SelectionBoxLayer
│   ├── DragPreviewLayer
│   └── MergePreviewLayer
├── MiniMap
├── ZoomController
└── CanvasToastLayer
```

## 8.3 Canvas Workspace 输入建议

```ts
interface CanvasWorkspaceProps {
  treeType: 'candidate' | 'book' | 'theme'
  treeId: string
  nodes: Record<string, BaseCanvasNode>
  rootNodeIds: string[]
  viewport: CanvasViewport
  selection: string[]
  mode: 'extraction' | 'pruning' | 'theme'
}
```

## 8.4 Canvas Workspace 不负责什么

- 不直接发 API
- 不直接持久化
- 不直接管理业务来源映射
- 不直接知道 QuoteCard / BookNode / ThemeNode 的业务差异细节

这些通过 adapter 层或 page action 层提供。

---

## 9. Adapter 层设计

## 9.1 为什么需要 Adapter

虽然三大页面共享画布，但它们的数据模型不同：
- CandidateNode
- BookNode
- ThemeNode

为了不让 CanvasWorkspace 被业务细节污染，建议在页面与画布之间增加 adapter 层。

## 9.2 Adapter 作用

- 将业务节点模型转成 BaseCanvasNode
- 将页面命令映射成统一 action
- 处理节点角标、状态、来源展示差异

## 9.3 Adapter 示例

### QuotesPageAdapter
- CandidateNode -> BaseCanvasNode
- CandidateEdge -> CanvasEdge

### BookTreePageAdapter
- BookNode -> BaseCanvasNode
- Book tree 派生 edges

### ThemeTreePageAdapter
- ThemeNode -> BaseCanvasNode
- Source mapping 转成角标或辅助信息

---

## 10. 状态管理分层

## 10.1 状态分层原则

前端状态建议按职责分为四层：

1. Server State
2. Document State
3. Canvas UI State
4. Global UI State

## 10.2 Server State

由 TanStack Query 管理，负责：
- books
- quotes
- quoteCards
- bookTrees
- themeTrees
- node details
- source mappings

### 特点
- 来源于服务端
- 可缓存
- 可失效重取
- 不适合承载高频本地编辑态

## 10.3 Document State

由 Zustand 管理，负责：
- 当前正在编辑的树结构
- 当前版本号
- 当前节点字典
- 当前 rootNodeIds
- 局部未提交改动

### 建议 store
- `useDocumentStore`

## 10.4 Canvas UI State

由 Zustand 管理，负责：
- 当前选中节点
- 多选节点
- 当前视口
- 当前缩放
- 当前模式
- 折叠状态
- 浮层状态

### 建议 store
- `useCanvasStore`

## 10.5 History State

由 Zustand 管理，负责：
- undo stack
- redo stack
- pending commands

### 建议 store
- `useHistoryStore`

## 10.6 Global UI State

由 Zustand 管理，负责：
- 左右面板展开收起
- Toast
- Modal
- 全局 command bar

### 建议 store
- `useUIStore`

---

## 11. 命令层挂载设计

## 11.1 命令层不能放在组件内部

命令层必须独立，避免：
- 组件直接改树
- 页面和画布同时维护命令逻辑
- 逻辑分散难维护

## 11.2 前端命令层建议结构

```txt
src/modules/command/
├── commandFactory.ts
├── commandExecutor.ts
├── optimisticApplier.ts
├── inverseCommand.ts（后期）
├── autosaveQueue.ts
└── validators/
```

## 11.3 命令流建议

用户操作后：
1. UI 事件触发
2. 页面 action 生成命令
3. commandExecutor 执行本地 optimistic update
4. historyStore 记录
5. autosaveQueue 提交 API

---

## 12. Query 层与 API 层设计

## 12.1 Query Hooks 组织建议

```txt
src/modules/api/
├── auth/
├── books/
├── quotes/
├── quoteCards/
├── bookTrees/
├── themeTrees/
├── reviews/
└── exports/
```

## 12.2 React Query Hooks 组织建议

```txt
src/modules/query/
├── useBooksQuery.ts
├── useQuotesQuery.ts
├── useQuoteCardQuery.ts
├── useBookTreeQuery.ts
├── useThemeTreeQuery.ts
├── useNodeDetailQuery.ts
└── useSourceMappingsQuery.ts
```

## 12.3 Query 层职责

- 请求后端
- 缓存服务端数据
- 提供 loading / error / stale 状态
- 不负责复杂业务改树

---

## 13. 目录结构建议

## 13.1 推荐目录结构

```txt
src/
├── app/
│   ├── router/
│   ├── providers/
│   └── layouts/
├── pages/
│   ├── BooksPage/
│   ├── QuotesPage/
│   ├── BookTreePage/
│   ├── ThemeTreePage/
│   ├── ReviewsPage/
│   └── SettingsPage/
├── modules/
│   ├── api/
│   ├── query/
│   ├── command/
│   ├── canvas/
│   ├── document/
│   ├── books/
│   ├── quotes/
│   ├── bookTree/
│   ├── themeTree/
│   └── review/
├── components/
│   ├── shell/
│   ├── shared/
│   ├── panels/
│   ├── cards/
│   └── feedback/
├── stores/
│   ├── useDocumentStore.ts
│   ├── useCanvasStore.ts
│   ├── useHistoryStore.ts
│   └── useUIStore.ts
├── lib/
│   ├── utils/
│   ├── constants/
│   └── types/
└── styles/
```

## 13.2 页面目录内部建议

例如：

```txt
pages/ThemeTreePage/
├── index.tsx
├── layout.tsx
├── adapter.ts
├── actions.ts
└── components/
```

---

## 14. 页面 Action 层设计

## 14.1 为什么需要 Action 层

页面不能直接把按钮点击事件写到命令层，也不能直接在组件里拼 API 调用。

需要一个 Action 层负责：
- 组合 UI 事件
- 调命令层
- 调 query invalidation
- 管理页面特有逻辑

## 14.2 示例

### QuotesPageActions
- onSelectQuote
- onSaveQuoteCard
- onPushToBookTree

### BookTreePageActions
- onAttachQuoteCard
- onMergeBookNodes
- onTagBookNodeTheme

### ThemeTreePageActions
- onBindSourceMapping
- onElevateAbstract
- onSetSourceBooks

---

## 15. 共享组件边界建议

## 15.1 应该共享的组件

### 壳层组件
- GlobalTopBar
- PrimarySidebar
- SecondaryResourcePanelShell

### 画布组件
- CanvasWorkspace
- CanvasToolbar
- FloatingToolPalette
- MiniMap
- ZoomController
- NodeRenderer（基类）
- EdgeRenderer

### 通用业务组件
- TabsContainer
- SearchInput
- FilterBar
- EmptyState
- ErrorState
- NotesCard
- ToastFeedback

## 15.2 不要过早共享的组件

以下组件不建议一开始强行共享：
- 金句页专属 CurrentQuoteCard
- 书内修剪台专属 QueueCardList
- 主题整理页专属 ThemeSourceCard

原因：
- 业务语义差异大
- 早期强行抽象容易让组件失真

原则：
**共享基础骨架，不共享过度业务化块。**

---

## 16. 右侧面板设计边界

## 16.1 右侧面板共性

三大核心页都需要右侧详情区，因此可以抽一个：
- `RightPanelShell`

负责：
- 宽度
- 收起展开
- tabs 容器
- 滚动区域

## 16.2 右侧面板差异化内容

### QuotesPage
- 提炼说明
- 候选主题
- 待定问题
- 备注

### BookTreePage
- 节点详情
- 修剪建议
- 来源
- 备注

### ThemeTreePage
- 主题定义
- 洞察
- 来源映射
- 相关主题
- 备注

---

## 17. 路由与上下文保持策略

## 17.1 页面切换原则

切换一级页面时，应尽量保留：
- 当前书籍
- 当前主题
- 当前节点
- 当前视口

## 17.2 URL 保持建议

使用 query 参数承载关键上下文：
- `bookId`
- `quoteId`
- `theme`
- `nodeId`

## 17.3 刷新恢复策略

页面刷新后优先从：
1. URL 参数
2. local cache / store snapshot
3. server context

恢复页面状态。

---

## 18. 第一阶段工程落地顺序

## 18.1 第一步：搭壳层

先完成：
- Router
- AppShell
- GlobalTopBar
- PrimarySidebar
- 页面空壳

## 18.2 第二步：搭三大页面骨架

先做：
- QuotesPage layout
- BookTreePage layout
- ThemeTreePage layout

先不做全部细节，先让左中右三区能稳定挂载。

## 18.3 第三步：搭 CanvasWorkspace

实现：
- 基础节点渲染
- 选中
- 拖拽
- 重命名
- 缩放平移
- 小地图

## 18.4 第四步：接 command layer

实现：
- renameNode
- createNode
- moveNode
- deleteNode
- mergeNodes
- elevateAbstract

## 18.5 第五步：接 API Query

逐步接入：
- quotes
- quote_cards
- book_trees
- theme_trees

---

## 19. 第一阶段必须落地的组件清单

## 19.1 壳层
- AppShell
- GlobalTopBar
- PrimarySidebar
- MainWorkspaceOutlet

## 19.2 页面
- QuotesPage
- BookTreePage
- ThemeTreePage

## 19.3 画布
- CanvasWorkspace
- CanvasToolbar
- MiniMap
- ZoomController
- NodeRenderer
- EdgeRenderer

## 19.4 store
- useDocumentStore
- useCanvasStore
- useHistoryStore
- useUIStore

## 19.5 命令层
- commandFactory
- commandExecutor
- autosaveQueue

---

## 20. 风险与注意事项

## 20.1 最大风险：页面直接侵入画布逻辑

如果每个页面都自己写一套节点拖拽和结构修改逻辑，后期一定失控。

## 20.2 第二风险：React Flow 成为真相源

React Flow 只能是渲染层，不能取代业务 tree。

## 20.3 第三风险：store 混乱

如果 document state、canvas state、history state 混在一起，后期会很难维护。

## 20.4 第四风险：过度抽象组件

一开始就试图把所有业务组件都抽象成超级通用组件，反而会让代码更难维护。

---

## 21. 一句话总结

这套前端组件树与页面路由设计的核心，不是“把页面拼出来”，而是围绕 **统一壳层、共享画布引擎、分层状态管理、独立命令系统** 四条主线，把产品文档真正翻译成一个可持续演进的前端工程骨架。