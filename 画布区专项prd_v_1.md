# 画布区专项 PRD（V1）

## 1. 文档信息

- 文档名称：画布区专项 PRD
- 所属产品：CogTree / 模型树阅读法
- 文档类型：专项 PRD
- 版本：V1.0
- 文档目标：统一定义三大核心页面共用的画布区能力，包括节点模型、关系模型、视口系统、拖拽规则、结构操作、快捷键、状态反馈与组件架构

---

## 2. 文档定位

### 2.1 一句话定义

画布区是整个模型树阅读法产品的**认知操作引擎**，用于承载用户从句子级局部结构，到书内知识树，再到跨书主题树的持续建构、修剪与系统思考。

### 2.2 为什么要单独做专项 PRD

虽然金句提炼页、书内修剪台、主题整理页的页面目标不同，但它们都依赖同一个核心能力：

- 节点编辑
- 关系表达
- 层级调整
- 结构修剪
- 视角导航
- 系统思考可视化

也就是说：

**三页不同，画布引擎共享。**

所以画布区不能只作为页面内的一个组件来看，而应被视为整个产品的底层交互系统。

### 2.3 画布区的角色

画布区不是展示区，而是：

- 结构建构区
- 因果表达区
- 修剪操作区
- 抽象提升区
- 系统思考区
- 关系调整区

---

## 3. 设计原则

## 3.1 画布优先

任何页面里，画布都应是最大、最连续、最稳定的区域。

## 3.2 思考不断流

用户应尽量在画布内连续完成核心操作，避免频繁弹窗、跳页、中断。

## 3.3 高频操作低成本

节点编辑、拖拽、合并、拆分、重命名、折叠、连线等操作路径必须尽可能短。

## 3.4 Tree First

树结构是唯一真相源，连线可由树结构派生；不能让边关系和层级关系长期双源并存。

## 3.5 视觉服务逻辑

画布的视觉呈现要优先体现：
- 层级
- 关系
- 当前焦点
- 待处理状态
- 来源状态

而不是追求纯装饰性。

## 3.6 允许不确定性

画布不仅承载稳定结构，也应允许：
- 待定节点
- 待验证关系
- 低信心节点
- 用户自建抽象节点

---

## 4. 画布区适用范围

本专项 PRD 适用于以下三个页面：

### 4.1 金句提炼页
- 句子级局部画布
- 局部因果候选结构
- 输出 CandidateNode / CandidateEdge

### 4.2 书内修剪台
- 单书结构画布
- 输出 BookNode / BookTree

### 4.3 主题整理页
- 跨书主题画布
- 输出 ThemeNode / ThemeTree

### 4.4 共用能力与差异说明

#### 共用能力
- 节点选中
- 拖拽
- 层级调整
- 连线
- 重命名
- 缩放 / 平移
- 小地图
- 折叠 / 展开
- 反馈提示

#### 差异点
- 金句页：局部提炼，不追求完整树
- 书内修剪台：强调吸收、合并、主题标记
- 主题整理页：强调跨书整合、来源映射、抽象提升

---

## 5. 画布区总体架构

## 5.1 画布区组成

画布区建议拆为以下 8 层：

1. 顶部画布控制条
2. 左侧悬浮工具栏
3. 主视图区（节点与关系）
4. 选区与交互预览层
5. 状态与角标层
6. 小地图层
7. 缩放与视图控制层
8. Toast / 操作反馈层

## 5.2 画布工作流

画布典型工作流：

1. 选中节点
2. 查看关系
3. 调整结构
4. 写入定义 / 来源 / 备注
5. 再回到画布继续整理

## 5.3 画布与页面其余区域的关系

- 左侧资源面板：给画布供料
- 右侧详情面板：解释画布中的当前节点
- 顶部全局栏：控制保存、撤销、导出、专注状态

所以：

**画布是主战场，左右面板都是服务画布的。**

---

## 6. 节点模型定义

## 6.1 节点类型分层

### 1）CandidateNode（候选节点）
用于金句提炼页的局部因果结构。

### 2）BookNode（书内节点）
用于书内修剪台的单书稳定节点。

### 3）ThemeNode（主题节点）
用于主题整理页的跨书主题节点。

## 6.2 节点共通字段

所有节点建议共享以下基础字段：

- id
- label
- nodeType
- parentId
- childrenIds
- orderIndex
- status
- shortDefinition
- createdAt
- updatedAt

## 6.3 节点语义类型 nodeType

建议统一支持以下枚举：

- concept：概念
- cause：原因
- effect：结果
- mechanism：中间机制
- question：问题
- note：备注型节点
- abstract：抽象层节点
- pending：待定节点

## 6.4 节点显示层级

节点除了语义类型外，还应有层级概念：

- root：根节点
- level1：一级节点
- level2：二级节点
- level3：三级节点
- leaf：叶子节点

## 6.5 节点状态 status

建议统一支持以下状态：

- normal：正常
- selected：选中
- editing：编辑中
- collapsed：已折叠
- pending_review：待整理
- pending_verify：待验证
- merge_candidate：可合并候选
- duplicated：疑似重复
- low_confidence：低信心
- has_note：有备注
- has_source：有来源
- referenced_multi_theme：多主题引用
- user_created_abstract：用户自建抽象
- hidden_by_filter：被过滤隐藏

## 6.6 节点角标建议

画布上的节点可显示轻量角标：

- 来源数
- 备注数
- 待验证数
- 折叠子节点数
- 多来源标记

---

## 7. 关系模型定义

## 7.1 关系类型

建议统一支持以下关系类型：

### 树结构关系
- tree_parent_child：父子关系

### 非树辅助关系
- support：支撑关系
- related：相关关系
- conflict：冲突关系
- pending_verify：待验证关系
- derived_from_source：来源映射关系

## 7.2 关系使用策略

### 金句提炼页
- 默认主要使用局部 parent-child 或 support 关系

### 书内修剪台
- 以 tree_parent_child 为主
- 辅助 support / pending_verify 为辅

### 主题整理页
- 以 tree_parent_child 为主
- 允许 related / derived_from_source 显示更丰富结构

## 7.3 关系显示规则

- 树关系：实线、主层级色
- 待验证关系：虚线
- 相关关系：弱化线
- 冲突关系：警示色但不刺眼
- 来源映射：可在特定模式下显示，不建议默认常显

---

## 8. 视口系统

## 8.1 视口字段

```ts
interface CanvasViewport {
  x: number
  y: number
  zoom: number
  fitMode?: 'manual' | 'fit_all' | 'fit_selection'
}
```

## 8.2 视口核心能力

- 缩放
- 平移
- 聚焦当前节点
- 适配整图
- 适配当前分支
- 恢复上次位置

## 8.3 视口交互规则

### 缩放
- 鼠标滚轮缩放，以光标所在位置为中心
- 工具栏按钮可离散缩放
- 双击节点可局部聚焦

### 平移
- 空格 + 拖拽平移
- 手型工具拖拽平移
- 小地图拖动视窗框平移

### 适配
- fit_all：适配整棵树
- fit_selection：适配当前节点/分支

## 8.4 小地图规则

### 功能
- 展示全局结构缩略图
- 展示当前视窗框
- 点击小地图跳转
- 拖动视窗框移动主画布

### 显示建议
- 默认常驻左下角或右下角
- 支持折叠

---

## 9. 画布核心交互能力

## 9.1 选中交互

### 单击节点
- 高亮当前节点
- 右侧加载详情
- 高亮上下游路径
- 小地图同步定位

### 单击关系线
- 高亮关系
- 右侧展示关系详情（如有）

### 多选
- Shift + 单击累积多选
- 框选进行区域多选

## 9.2 编辑交互

### 双击节点
- 原地重命名
- Enter 保存
- Esc 取消

### 悬停节点
显示轻量工具：
- 新增子节点
- 新增同级节点
- 连线
- 添加备注
- 更多

### 右键节点
弹出上下文菜单：
- 重命名
- 合并
- 拆分
- 提升抽象
- 删除
- 添加来源
- 标记待验证
- 折叠 / 展开

## 9.3 拖拽交互

### 节点拖拽
支持：
- 改父子层级
- 同级排序
- 子树整体移动

### 拖拽预反馈
必须有：
- 目标节点高亮
- 插入位置线
- 非法目标提示
- 循环引用禁止提示

## 9.4 连线交互

### 创建关系
- 从节点连接点拖向目标节点
- 松手后创建关系
- 若当前模式需要，可弹轻量关系类型菜单

### 删除关系
- 选中关系后 Delete 删除
- 删除前可显示关系摘要

## 9.5 折叠交互

### 折叠
- 节点折叠后仅显示自身
- 子树隐藏
- 显示折叠计数角标

### 展开
- 点击折叠标识恢复显示子树

---

## 10. 结构操作能力

## 10.1 新增节点

### 新增同级节点
- 快捷键 Enter
- 或节点悬浮菜单触发

### 新增子节点
- 快捷键 Tab
- 或节点悬浮菜单触发

### 新建根级节点
- 在空白区通过工具栏触发

## 10.2 重命名

- 双击节点进入编辑
- 编辑时节点宽度自适应
- 保存后触发自动保存

## 10.3 删除节点

- Delete / Backspace 删除
- 默认软删除
- 删除父节点时，需明确子节点处理策略

### 子节点处理策略建议
- 一并删除整棵子树
- 或提升一层（后期可选）

## 10.4 合并节点

### 适用场景
- 重复节点
- 近义节点
- 多来源相似节点

### 触发方式
- 多选节点后点击“合并”
- 或右键菜单触发

### 合并流程
1. 多选节点
2. 触发合并
3. 轻量确认层：
   - 选择保留名
   - 自定义新名称
   - 是否保留全部来源
4. 合并完成

### 合并结果
- 继承所有来源
- 继承备注与待验证问题
- 清理重复关系

## 10.5 拆分节点

### 适用场景
- 节点语义过大
- 多概念混杂
- 一个节点里其实有多个子概念

### 拆分流程
1. 选中节点
2. 点击“拆分”
3. 输入新节点列表
4. 重新分配子节点与来源

## 10.6 提升抽象

### 适用场景
- 多个同层节点可归并为更高层概念

### 触发条件
- 默认只允许同层节点触发

### 流程
1. 多选同层节点
2. 点击“提升抽象”
3. 创建新上位节点
4. 原节点自动成为其子节点

### 验证规则
- 跨层节点默认禁止提升抽象
- 若跨层触发，提示“仅支持同层节点提升抽象”

## 10.7 批量操作

支持：
- 批量移动
- 批量合并
- 批量标记待验证
- 批量删除
- 批量加入某主题（书内修剪台 / 主题整理页）

---

## 11. 页面差异化规则

## 11.1 金句提炼页画布规则

### 定位
- 局部提炼画布
- 输出候选结构

### 特点
- 规模小
- 不追求全树完整性
- 更强调低阻力提炼

### 支持重点
- 候选节点
- 候选关系
- 多因多果表达
- 候选主题标记

## 11.2 书内修剪台画布规则

### 定位
- 单书结构收敛画布

### 特点
- 重点在吸收 QuoteCard
- 重点在合并与修剪
- 形成 BookTree

### 支持重点
- 从待整理池拖入节点
- 标主主题 / 次主题
- 查看来源 QuoteCard
- 结构修剪建议

## 11.3 主题整理页画布规则

### 定位
- 跨书主题整合画布

### 特点
- 重点在跨书整合
- 重点在来源映射
- 重点在用户自建抽象

### 支持重点
- 来源书籍切换
- 来源节点映射
- 跨书节点整合
- 用户抽象节点创建

---

## 12. 画布顶部工具条

## 12.1 共用控件

- 布局切换
- 缩放比例
- 适配视图
- 只看当前分支（后期）
- 聚焦当前节点

## 12.2 页面专属按钮建议

### 金句提炼页
- 保存 QuoteCard
- 送入书内整理池

### 书内修剪台
- 合并节点
- 提升抽象
- 拆分
- 删除冗余
- MECE 检查

### 主题整理页
- 合并节点
- 提升抽象
- 查看来源映射
- 导出主题摘要

---

## 13. 左侧悬浮工具栏

## 13.1 建议工具项

- 选择工具
- 手型拖动工具
- 连线工具
- 文本/节点工具
- 备注工具
- 框选工具（后期）
- 更多

## 13.2 交互原则

- 工具数量尽量少而稳定
- 默认以选择工具为主
- 高级工具放入“更多”或后期启用

---

## 14. 快捷键体系

## 14.1 基础快捷键

- Enter：新增同级节点
- Tab：新增子节点
- Shift + Tab：提升一级
- Delete / Backspace：删除节点
- Cmd/Ctrl + Z：撤销
- Cmd/Ctrl + Shift + Z：重做
- Space：临时手型平移
- Esc：退出当前编辑态

## 14.2 建议快捷键

- Cmd/Ctrl + F：搜索当前页
- Cmd/Ctrl + K：命令面板（后期）
- Cmd/Ctrl + A：全选当前分支（后期）
- F：聚焦当前节点（可选）
- M：合并（多选后可用，可选）

---

## 15. 状态反馈系统

## 15.1 反馈类型

### 操作成功
- 已移动到“增长财富”下
- 已合并 2 个节点为“股权”
- 已加入书内整理池

### 操作失败
- 不能拖拽到自己的子节点下
- 当前节点层级不允许提升抽象
- 当前结构保存失败，请重试

### 过程提示
- 自动保存中…
- 已保存
- 检测到 3 个疑似重复节点

## 15.2 视觉反馈层级

- 一级：Toast 提示
- 二级：节点局部高亮动画
- 三级：顶部状态文字
- 四级：危险操作确认层

---

## 16. 空状态与异常状态

## 16.1 空状态

### 空画布
根据页面不同给出引导：

#### 金句提炼页
- 从当前金句中提关键词并生成候选节点

#### 书内修剪台
- 从待整理池拖入第一张因果卡

#### 主题整理页
- 先选择来源书籍或手动创建主题根节点

## 16.2 异常状态

- 画布数据加载失败
- 自动保存失败
- 撤销 / 重做失败
- 来源映射加载失败
- 树结构校验失败

### 异常处理原则
- 不轻易清空当前画布显示
- 可重试
- 错误提示清楚但不打断思路

---

## 17. 数据结构建议

## 17.1 通用节点接口

```ts
interface BaseCanvasNode {
  id: string
  label: string
  nodeType: 'concept' | 'cause' | 'effect' | 'mechanism' | 'question' | 'note' | 'abstract' | 'pending'
  parentId?: string | null
  childrenIds: string[]
  orderIndex: number
  shortDefinition?: string
  status: string
  createdAt: string
  updatedAt: string
}
```

## 17.2 通用关系接口

```ts
interface CanvasEdge {
  id: string
  sourceId: string
  targetId: string
  relationType: 'tree_parent_child' | 'support' | 'related' | 'conflict' | 'pending_verify' | 'derived_from_source'
  status?: string
}
```

## 17.3 视口接口

```ts
interface CanvasViewport {
  x: number
  y: number
  zoom: number
  fitMode?: 'manual' | 'fit_all' | 'fit_selection'
}
```

---

## 18. 组件清单

## 18.1 核心组件

- CanvasWorkspace
- CanvasViewportLayer
- CanvasToolbar
- FloatingToolPalette
- MiniMap
- ZoomController
- LayoutSwitcher
- ToastFeedbackLayer

## 18.2 节点与关系组件

- NodeRenderer
- NodeBadgeLayer
- EdgeRenderer
- EdgeLabelLayer（后期）
- SelectionBox
- DragPreviewLayer
- MergePreviewLayer

## 18.3 交互组件

- ContextMenu
- InlineEditor
- RelationTypePopover
- ConfirmPopover
- CommandBar（后期）

---

## 19. 接口建议

## 19.1 必需接口

- 获取树数据
- 保存树数据变更
- 更新节点
- 更新关系
- 更新视口
- 撤销 / 重做
- 获取节点详情
- 获取来源映射

## 19.2 存储建议

- 采用 Tree First 存储
- 关系可派生，不作为最终真相源
- 节点状态单独存储
- 自动保存采用增量 patch 更新

---

## 20. MVP 范围

## 20.1 MVP 必须实现

- 节点单击 / 双击 / 拖拽
- 原地重命名
- 新增同级 / 子节点
- 删除节点
- 缩放 / 平移 / 小地图
- 折叠 / 展开
- 合并节点
- 提升抽象
- 自动保存
- 撤销 / 重做
- 基础 Toast 反馈

## 20.2 第二阶段建议实现

- 拆分节点
- 多选与批量操作
- 关系类型扩展
- 只看当前分支
- 结构建议面板联动

## 20.3 第三阶段建议实现

- AI 合并建议
- AI 抽象提升建议
- AI 结构检查
- 命令面板
- 协作编辑

---

## 21. 验收标准

### 功能验收
1. 三个页面可共用同一套基础画布交互能力
2. 节点可稳定完成新增、删除、重命名、拖拽与层级调整
3. 合并、提升抽象可用且结果可回滚
4. 缩放、平移、小地图可支撑中大型树结构编辑
5. 自动保存、撤销、重做稳定可用

### 体验验收
1. 用户能连续工作，不被频繁打断
2. 高频操作路径明显短于传统表单式编辑
3. 画布能清楚表达层级与当前焦点
4. 左右侧栏不会压制画布主地位
5. 三页虽然目标不同，但操作心智一致

---

## 22. 一句话总结

画布区不是页面里的一个普通区域，而是模型树阅读法产品的**核心交互引擎**；它决定了用户能否顺畅地把句子级提炼、书内收敛与跨书整合，真正转化为持续生长的知识体系。